// 4) Write a program to create an enum called "DaysOfWeek"
//  representing the days of the week using enum and display it
enum dayweek {
  //defination enum
   Satrday, Sunday, Monday, Tuesday,
    Wednesday, Thursday, Friday }

main() {
  // print enum
 print(dayweek.values);
}
